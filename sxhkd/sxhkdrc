# Ake sxhkd reload its configuration files:
super + Delete
	pkill -USR1 -x sxhkd && dunstify -u critical "sxhkd" "Configuration reloaded!"


# BSPWM HOTKEYS

# quit/restart bspwm
super + shift + {Delete, r}
	bspc {quit,wm -r}

# close and kill
super + q
	bspc node -c

# alternate between the tiled and monocle layout
super + m
	bspc desktop -l next

# set the window state
super + {t,shift + t,space,f}
	bspc node -t {tiled,pseudo_tiled,~floating,~fullscreen}

# FOCUS/SWAP

# focus the node in the given direction
super + {_,shift + }{h,j,k,l}
	bspc node -{f,s} {west,south,north,east}

# focus the next/previous node in the current desktop
super + {_,shift + }n
	bspc node -f {next,prev}.local.!hidden.window

# focus or send to the given desktop
super + {_,shift + }{1,2,3,4,8,9,0}
	bspc {desktop -f,node -d} { , , , , , , }

a MOVE/RESIZE
#
# move a floating window

super + {Left,Down,Up,Right}
	bspc node -v {-20 0,0 20,0 -20,20 0}


# GENERAL
XF86AudioRaiseVolume
	amixer set Master 5%+

XF86AudioLowerVolume
	amixer set Master 5%-

XF86AudioPlay
	mpc toggle

XF86AudioPrev
	mpc prev

XF86AudioNext
	mpc next

XF86AudioMute
	micro --toggle

super + XF86AudioPlay

F1
	micro --toggle

XF86MonBrightnessUp
	brightness -i
	
XF86MonBrightnessDown
	brightness -d
	

# APPLICATION KEYBINDINGS
super + b
			qutebrowser
super + r
			kitty -e yazi
super + Return
			kitty
super + d
			rofi -config ~/.config/rofi/config.rasi -show drun
super + g 
			rofi -config ~/.config/rofi/config.rasi -show drun -drun-categories Game
super + s
			rofi -config ~/.config/rofi/config.rasi -show run
super + c
			rofi -show calc -modi calc -no-show-match -no-sor 
super + x
			betterlockscreen -l
super + shift + x
	  		systemctl suspend
super + w
			kitty -e newsboat

super + p
	 /home/oleg/dotfiles/scripts/pswd-cl

super + grave 
	tdrop -ma -y 18 --wm "bspwm" kitty 

super + Print
    scrot ~/HDrive/Media/Pictures/Screenshots/%Y-%m-%d-%T-screenshot.png && dunstify -u normal "scrot" "screenshot saved"

Print
    scrot ~/%Y-%m-%d-%T-screenshot.png -s && dunstify -u normal "scrot" "screenshot saved"
    

#poe related

F3
    poe_logout
super + F3
    java -jar /home/oleg/Programms/cadiro.jar -l Hardcore//Ancestor
